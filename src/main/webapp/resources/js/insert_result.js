/**
 * Created by Alex on 07.05.2017.
 */
function insertResult(result) {
    if (result === 1){
        alert("Insertion successful!");
    } else if (result === 0){
        alert("Your Input is incorrect, try again!");
    }
}
// window.onload = insertResult(${insRes});
